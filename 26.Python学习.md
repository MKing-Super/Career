# Python

查看版本：

```
python --version
```

vscode增加扩展：

```bash
# 核心引擎
Python

# 智能补全
Pylance

# 交互式笔记本
Jupyter

# 代码追溯
GitLens

# 数据预览
Rainbow CSV

# 表格预览
Excel Viewer

# 行内报错
Error Lens

# 拼写检查
Code Spell Checker

# 文档生成
autoDocstring

```

## 使用虚拟环境

​		虚拟环境可以为每个项目单独安装库，避免不同项目之间的版本冲突。这很像 Java 的 Maven 项目管理依赖。

**步骤 1：创建并激活虚拟环境**

**1 打开终端**：在 VS Code 中，按 `Ctrl + `` 打开终端。

**2 创建虚拟环境**：在终端中输入以下命令： 

```
python -m venv .venv
```

这会在当前项目文件夹下创建一个名为 `.venv`的虚拟环境目录。

**3 激活虚拟环境**：

**Windows (CMD)**：

```
.venv\Scripts\activate.bat
```

**激活成功的标志**：你的终端提示符前面会显示 `(.venv)`，如下图：

```
(.venv) PS C:\your_project_path>
```

**步骤 2：在虚拟环境中安装库**

确保终端提示符前有 `(.venv)`后，执行以下命令：

```
pip install pandas numpy
```

`pip`是 Python 的包管理器，会自动从 PyPI（Python 官方软件仓库）下载并安装库及其依赖。

**可选**：如果你需要特定版本（如量化项目有时对版本有要求），可以指定版本：

```
pip install pandas==2.1.4 numpy==1.24.0
```

**步骤 3：验证安装**

在激活的虚拟环境中，运行 Python 解释器并尝试导入库：

```

python
```

然后在出现的 `>>>`交互式提示符下输入：

```

import pandas as pd
import numpy as np
print(pd.__version__)
print(np.__version__)
```

如果没有报错，并且能正确打印出版本号（如 `2.1.4`和 `1.24.0`），则安装成功。输入 `exit()`退出 Python 环境。

**步骤 4：在 VS Code 中选用此环境**

为了让 VS Code 识别并使用这个虚拟环境：

1. 按 `Ctrl+Shift+P`打开命令面板。
2. 输入 `Python: Select Interpreter`并选择。
3. 在列表中选择以 `./.venv`或 `./.venv/Scripts/python.exe`开头的解释器。









