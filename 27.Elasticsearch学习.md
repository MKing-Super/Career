# Elasticsearch

## 安装

elasticsearch-9.3.3 & kibana-9.3.3

```
https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-9.3.3-windows-x86_64.zip

https://artifacts.elastic.co/downloads/kibana/kibana-9.3.3-windows-x86_64.zip
```

### elasticsearch

config/elasticsearch.yml

```yaml
# ======================== Elasticsearch Configuration =========================
#
# Elasticsearch 配置文件
# 中文注释版
#

# ---------------------------------- 基本配置 -----------------------------------

# 集群名称
cluster.name: springai-demo

# 节点名称
node.name: es-node-1

# ---------------------------------- 网络配置 -----------------------------------

# 绑定地址 (127.0.0.1 仅本地访问，0.0.0.0 支持外网访问)
network.host: 127.0.0.1

# HTTP 端口
http.port: 9200

# ---------------------------------- 集群配置 -----------------------------------

# 单节点模式 (无需其他节点即可启动)
discovery.type: single-node

# ---------------------------------- 安全配置 -----------------------------------

# 禁用安全功能 (生产环境建议开启)
xpack.security.enabled: false

```

新增 ./start.bat

```bash
@echo off
set JAVA_HOME=
cd /d D:\elasticsearch-9.3.3
start "Elasticsearch" bin\elasticsearch.bat
```

### kibana

kibana.yml

```yaml
# Kibana 服务端口
server.port: 5601
# Kibana 绑定地址
server.host: "127.0.0.1"
# Elasticsearch 连接地址
elasticsearch.hosts: ["http://localhost:9200"]
```

start.bat

```bat
@echo off
cd /d D:\kibana-9.3.3
start "Kibana" bin\kibana.bat
```

页面url

```
127.0.0.1:5601
```



## 基本操作

### 1. 创建索引（类似建表）

```

PUT /test_demo
{
  "settings": {
    "number_of_shards": 1
  },
  "mappings": {
    "properties": {
      "title": { "type": "text" },
      "content": { "type": "text" },
      "create_time": { "type": "date" }
    }
  }
}
```

**说明**：`test_demo`是索引名（相当于数据库表）。执行后右侧应返回 `"acknowledged" : true`。

### 2. 插入文档（增 - Create）

**方式 A：指定 ID（ID=1）**

```

POST /test_demo/_doc/1
{
  "title": "Hello Kibana",
  "content": "这是我的第一条测试数据",
  "create_time": "2026-04-21T10:00:00"
}
```

**方式 B：自动生成 ID（更常用）**

```

POST /test_demo/_doc
{
  "title": "自动ID文档",
  "content": "系统会自动分配一个唯一ID"
}
```

**注意**：Kibana 中 `POST`用于新增，`PUT`用于创建索引或全量替换。

### 3. 查询文档（查 - Read）

**查询指定 ID（ID=1）**

```

GET /test_demo/_doc/1
```

**条件搜索（全文检索）**

```

GET /test_demo/_search
{
  "query": {
    "match": {
      "title": "Hello"
    }
  }
}
```

这会搜索 `title`字段中包含 "Hello" 的文档。

### 4. 更新文档（改 - Update）

**局部更新**（只改指定字段，不覆盖整条数据）

```

POST /test_demo/_update/1
{
  "doc": {
    "content": "这是更新后的内容"
  }
}
```

**注意**：更新必须使用 `_update`API 和 `doc`包裹。

### 5. 删除文档（删 - Delete）

```

DELETE /test_demo/_doc/1
```

### 6. 删除整个索引（危险操作）

```

DELETE /test_demo
```

**警告**：这会删除 `test_demo`索引及其**所有数据**，不可恢复，慎用！



### 常用辅助命令

```
// 查看集群健康（green/yellow/red）
GET /_cluster/health?pretty

// 查看所有索引列表
GET /_cat/indices?v

// 查看索引结构（mapping）
GET /test_demo/_mapping
```

