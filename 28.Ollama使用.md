# Ollama安装与使用

## 安装

1 下载地址

```
官方：
https://ollama.com/download

国内源：
https://cnb.cool/hex/ollama/-/releases
```

> 下载url：
>
> 官方：
>
> https://release-assets.githubusercontent.com/github-production-release-asset/658928958/f8eeb315-0e77-457e-9415-1ea449c39e07?sp=r&sv=2018-11-09&sr=b&spr=https&se=2026-04-23T01%3A48%3A52Z&rscd=attachment%3B+filename%3DOllamaSetup.exe&rsct=application%2Foctet-stream&skoid=96c2d410-5711-43a1-aedd-ab1947aa7ab0&sktid=398a6654-997b-47e9-b12b-9515b896b4de&skt=2026-04-23T00%3A47%3A58Z&ske=2026-04-23T01%3A48%3A52Z&sks=b&skv=2018-11-09&sig=AqxMVfmRGiD48Eg9CiqsF%2Fk0Ic%2B%2B4LAqiL4ERM%2F54eA%3D&jwt=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpc3MiOiJnaXRodWIuY29tIiwiYXVkIjoicmVsZWFzZS1hc3NldHMuZ2l0aHVidXNlcmNvbnRlbnQuY29tIiwia2V5Ijoia2V5MSIsImV4cCI6MTc3NjkxMDMxMiwibmJmIjoxNzc2OTA2NzEyLCJwYXRoIjoicmVsZWFzZWFzc2V0cHJvZHVjdGlvbi5ibG9iLmNvcmUud2luZG93cy5uZXQifQ.RS4_JFHO3s9JO07T8NMvFv2W_u_5YMZpY996AP696qo&response-content-disposition=attachment%3B%20filename%3DOllamaSetup.exe&response-content-type=application%2Foctet-stream
>
> 
>
> 国内源：
>
> https://asset.cnb.cool/assets/t/dfKwQN6ejcJCCc16c8Z0o4kXxyP



下载 `OllamaSetup.exe`。直接双击运行，默认安装即可（它会自动注册系统服务并设置环境变量）。

2 验证

```
ollama --version
```

3 （关键）修改模型存储路径（防爆 C 盘）

Ollama 默认下载到 C 盘用户目录。为防止 C 盘空间不足，建议安装后立即设置环境变量： 

- 右键“此电脑” → “属性” → “高级系统设置” → “环境变量” 
- 在“系统变量”中点击“新建”： 
- **变量名**：`OLLAMA_MODELS` 
- **变量值**：`D:\ollama_models`（路径自定义，确保磁盘空间充足） 
- **重启电脑**或重启“Ollama”服务使配置生效。

启动ollama：

**方法一：图形化启动（推荐）**

1. 按 `Win`键，直接搜索输入 `Ollama`。
2. 点击出现的 **Ollama Desktop** 或 **Ollama** 应用。
3. 等待任务栏右下角出现鲸鱼图标，表示服务已启动。
4. 回到 PowerShell 再次输入 `ollama list`，应该就能正常显示了。

**方法二：命令行启动**

如果你喜欢纯命令行，可以在当前 PowerShell 窗口直接运行：

```
ollama serve
```

这个命令会**前台启动**服务（窗口不能关），之后新开一个终端窗口就能正常使用 `ollama run`等命令了。



## 拉取模型

**1 拉取并配置 Chat 模型 (Qwen2.5:0.5B)**

**拉取模型** 在 PowerShell 中执行（国内网络建议使用此命令，利用镜像加速）： 

```
$env:OLLAMA_MODEL_SERVER="https://ollama.modelscope.cn"
ollama pull qwen2.5:0.5b
```

> 模型大小约 300 MB，下载速度极快。

**功能测试**

```
ollama run qwen2.5:0.5b
```

输入一段中文（如“你好”），能正常回复即说明模型加载成功。按 `Ctrl+D`退出对话。



**2 拉取并配置 Embedding 模型 (nomic-embed-text)**

**拉取模型**

```
ollama pull nomic-embed-text
```

> 模型大小约 274 MB，专用于生成向量，不支持对话。

**API 接口验证**

保持 Ollama 服务运行，在 PowerShell 中使用 `curl`测试（需 Windows 10/11 内置的 curl）：

```
curl http://localhost:11434/api/embeddings -H "Content-Type: application/json" -d '{"model": "nomic-embed-text", "prompt": "Hello world"}'
```

如果返回一长串数字（向量数组），说明 Embedding 服务正常。



## SpringAi项目配置

在你的 `application.yml`中，使用以下配置即可完成对接：

```yml
spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: qwen2.5:0.5b
      embedding:
        model: nomic-embed-text
```



## 服务管理与常见问题

**开机自启**：Ollama 安装为系统服务，开机即自动运行，无需手动干预。

**查看已安装模型**：`ollama list`

**端口冲突**：如果 11434 端口被占用，可尝试 `ollama serve`重启服务，或修改 `OLLAMA_HOST`环境变量。 

**避坑指南**：如果拉取模型极慢或失败，请确保 `OLLAMA_MODEL_SERVER`环境变量已正确设置为 `https://ollama.modelscope.cn`（国内镜像源）。